# 📁 فهرست کامل فایل‌های پروژه
## Military Flight Tracker Bot v15.0

---

## 🎯 فایل‌های اصلی (الزامی برای اجرا)

### 1. `bott.py` (50 KB)
**فایل اصلی ربات**
- حاوی تمام منطق اصلی
- ادغام Gemini AI
- مدیریت پروازها
- ارسال اعلان‌های تلگرام
- 1,322 خط کد

**استفاده:**
```bash
python bott.py
```

---

### 2. `database.py` (22 KB)
**مدیریت پایگاه داده هوشمند**
- 4 جدول SQLite
- سیستم یادگیری و کش
- جستجوی چندلایه
- مدیریت آمار
- 537 خط کد

**ویژگی‌ها:**
- `FlightDatabase` class
- یادگیری خودکار از AI
- امتیاز اعتماد (Confidence Score)
- پاکسازی خودکار

---

### 3. `config.py` (2.0 KB)
**فایل تنظیمات**
- توکن ربات تلگرام
- آیدی کانال
- کلیدهای API Gemini
- پارامترهای ربات
- 51 خط تنظیمات

**⚠️ مهم:** این فایل را ویرایش کرده و اطلاعات خود را وارد کنید!

---

## 📦 فایل‌های نصب و راه‌اندازی

### 4. `requirements.txt` (296 bytes)
**وابستگی‌های Python**

محتوا:
```
python-telegram-bot==21.7
requests==2.32.3
urllib3==2.2.3
```

**نصب:**
```bash
pip install -r requirements.txt
```

---

### 5. `verify_setup.py` (پیشنهادی)
**اسکریپت تایید نصب**

بررسی می‌کند:
- ✅ نسخه Python
- ✅ کتابخانه‌های نصب شده
- ✅ فایل config.py
- ✅ اعتبار توکن تلگرام
- ✅ کلیدهای Gemini
- ✅ اتصال به API ها

**استفاده:**
```bash
python verify_setup.py
```

---

## 📚 فایل‌های مستندات

### 6. `README.md` (11 KB)
**راهنمای کامل پروژه (فارسی)**

شامل:
- معرفی کامل قابلیت‌ها
- راهنمای نصب گام به گام
- توضیح تنظیمات
- دستورات ربات
- عیب‌یابی
- نکات امنیتی

**برای چه کسانی:** همه کاربران

---

### 7. `QUICKSTART.md` (7.3 KB)
**راهنمای سریع شروع کار (5 دقیقه)**

شامل:
- نصب سریع Python
- دریافت کلیدها
- تنظیم config.py
- اجرای ربات
- تست
- رفع مشکلات متداول

**برای چه کسانی:** مبتدی‌ها و کسانی که عجله دارند

---

### 8. `PROJECT_SUMMARY.md` (15 KB)
**خلاصه فنی کامل پروژه**

شامل:
- معماری سیستم
- جزئیات پیاده‌سازی
- آمار کد
- تکنولوژی‌ها
- بهبودها نسبت به نسخه قبل
- ایده‌های توسعه

**برای چه کسانی:** توسعه‌دهندگان و کاربران پیشرفته

---

### 9. `FILES_OVERVIEW.md` (این فایل)
**فهرست کامل فایل‌ها**

---

## 🔧 فایل‌های کمکی

### 10. `config.example.py` (2.9 KB)
**نمونه فایل تنظیمات**

- الگویی برای config.py
- شامل تمام گزینه‌ها با توضیحات
- مقادیر نمونه

**استفاده:**
```bash
cp config.example.py config.py
# سپس config.py را ویرایش کنید
```

---

## 📄 فایل‌های سیستم

### 11. `LICENSE`
**مجوز MIT**
- پروژه متن‌باز
- قابل استفاده آزاد

---

## 🗃️ فایل‌های خودکار (ایجاد می‌شوند)

این فایل‌ها هنگام اجرای ربات خودکار ساخته می‌شوند:

### `flight_tracker.db`
**پایگاه داده SQLite**
- حاوی تمام اطلاعات یادگرفته شده
- جداول: spotted_flights, aircraft_intelligence, unidentified_flights, api_stats
- رشد مداوم در طول زمان

### `bot.log`
**فایل لاگ**
- تمام رویدادهای ربات
- خطاها و هشدارها
- مفید برای عیب‌یابی

---

## 📊 ساختار کلی پروژه

```
military-flight-tracker/
│
├── 🚀 فایل‌های اصلی (3 فایل - الزامی)
│   ├── bott.py              (فایل اصلی ربات)
│   ├── database.py          (مدیریت دیتابیس)
│   └── config.py            (تنظیمات - باید ویرایش شود)
│
├── 📦 نصب و راه‌اندازی
│   ├── requirements.txt     (وابستگی‌ها)
│   └── verify_setup.py      (تست نصب)
│
├── 📚 مستندات (4 فایل)
│   ├── README.md            (راهنمای کامل)
│   ├── QUICKSTART.md        (راهنمای سریع)
│   ├── PROJECT_SUMMARY.md   (خلاصه فنی)
│   └── FILES_OVERVIEW.md    (این فایل)
│
├── 🔧 کمکی
│   ├── config.example.py    (نمونه تنظیمات)
│   └── LICENSE              (مجوز)
│
└── 🗃️ خودکار (هنگام اجرا)
    ├── flight_tracker.db    (دیتابیس)
    └── bot.log              (لاگ)
```

---

## 🎯 چک‌لیست فایل‌های مورد نیاز

برای اجرای موفق ربات، این فایل‌ها را بررسی کنید:

- [x] `bott.py` - موجود
- [x] `database.py` - موجود
- [ ] `config.py` - **باید ایجاد/ویرایش شود!**
- [x] `requirements.txt` - موجود
- [x] مستندات (README.md, etc.) - موجود

---

## 📝 مراحل شروع کار

### گام 1: بررسی فایل‌ها
```bash
ls -lh
# باید 10+ فایل ببینید
```

### گام 2: نصب وابستگی‌ها
```bash
pip install -r requirements.txt
```

### گام 3: تنظیم config.py
```bash
# اگر وجود ندارد:
cp config.example.py config.py

# سپس ویرایش:
nano config.py  # یا vi یا هر ادیتور دیگر
```

### گام 4: تایید نصب
```bash
python verify_setup.py
```

### گام 5: اجرا
```bash
python bott.py
```

---

## 📖 کدام فایل را بخوانم؟

### اگر تازه شروع کرده‌اید:
👉 **QUICKSTART.md**

### اگر می‌خواهید همه چیز را بدانید:
👉 **README.md**

### اگر توسعه‌دهنده هستید:
👉 **PROJECT_SUMMARY.md**

### اگر مشکلی پیش آمد:
👉 **README.md** (بخش عیب‌یابی) + **bot.log**

### اگر می‌خواهید کد را درک کنید:
👉 **bott.py** و **database.py** (خود فایل‌ها با کامنت کامل)

---

## 🔍 جستجو در فایل‌ها

### برای پیدا کردن تنظیمات:
```bash
grep -n "POLL_INTERVAL" config.py
```

### برای پیدا کردن یک تابع:
```bash
grep -n "def analyze_flight" bott.py
```

### برای مشاهده ساختار دیتابیس:
```bash
grep -n "CREATE TABLE" database.py
```

---

## 💾 حجم کل پروژه

```
فایل‌های کد:        ~74 KB
فایل‌های مستندات:   ~33 KB
فایل‌های دیگر:      ~3 KB
──────────────────────────
جمع کل:            ~110 KB
```

**نکته:** این یک پروژه بسیار سبک است!

---

## 🔐 فایل‌هایی که نباید در Git قرار بگیرند

اگر می‌خواهید این پروژه را در GitHub بگذارید، این فایل‌ها را به `.gitignore` اضافه کنید:

```gitignore
# تنظیمات (حاوی کلیدهای محرمانه)
config.py

# دیتابیس
*.db
*.db-journal

# لاگ‌ها
*.log

# Python cache
__pycache__/
*.pyc
*.pyo
*.pyd

# محیط مجازی
venv/
env/
```

---

## 📤 اشتراک‌گذاری پروژه

### برای اشتراک‌گذاری با دیگران:

**شامل شود:**
- ✅ تمام فایل‌های .py (به جز config.py)
- ✅ تمام فایل‌های .md
- ✅ requirements.txt
- ✅ config.example.py
- ✅ LICENSE

**نباید شامل شود:**
- ❌ config.py (حاوی کلیدها)
- ❌ flight_tracker.db (داده‌های شخصی)
- ❌ bot.log (لاگ‌های شخصی)

---

## 🆘 راهنمای سریع عیب‌یابی

### ربات اجرا نمی‌شود:
1. بررسی `verify_setup.py`
2. مطالعه `bot.log`

### پیام‌ها ارسال نمی‌شوند:
1. بررسی `config.py`
2. مطالعه بخش عیب‌یابی در `README.md`

### دیتابیس خراب شده:
```bash
rm flight_tracker.db
python bott.py  # دیتابیس جدید ایجاد می‌شود
```

---

## 📞 منابع بیشتر

- **کد منبع کامل:** تمام فایل‌های .py
- **API Documentation:** نظرات داخل کد
- **مثال‌های عملی:** در README.md و QUICKSTART.md

---

## ✨ نکته پایانی

همه فایل‌ها با دقت طراحی شده‌اند. هیچ فایل اضافی یا غیرضروری وجود ندارد. هر فایل یک هدف مشخص دارد.

**توصیه:** قبل از اجرا، حتماً `verify_setup.py` را اجرا کنید!

---

**تاریخ ایجاد:** 2025-10-24  
**نسخه:** 15.0  
**تعداد کل فایل‌ها:** 11 فایل

**موفق باشید! 🚀**
